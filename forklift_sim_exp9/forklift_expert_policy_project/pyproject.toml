[project]
name = "forklift-expert"
version = "0.1.0"
description = "Rule-based expert policy + demo collection for IsaacLab forklift pallet insertion task."
requires-python = ">=3.10"
dependencies = ["numpy"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
